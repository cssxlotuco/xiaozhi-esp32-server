# --- 1. General Settings ---
DOMAIN=binai.ddns.net
SERVER_HOST=${DOMAIN}
HTTP_PORT=80
HTTPS_PORT=443
LOG_LEVEL=INFO

# --- 2. MQTT Broker (If using an external broker) ---
# Set to the host of your MQTT server (e.g., your server IP or a public broker)
MQTT_HOST=mqtt.server.com
MQTT_PORT=1883
MQTT_USER=your_mqtt_user
MQTT_PASSWORD=your_mqtt_password

# --- 3. TLS/SSL Configuration (For HTTPS) ---
# Paths inside the Docker container, mount certs to these paths
TLS_CERT=/etc/nginx/ssl/server.crt
TLS_KEY=/etc/nginx/ssl/server.key

# --- 4. Database (If applicable, e.g., for persistent storage) ---
# Assuming a default setup, confirm required variables from code/README
# DB_HOST=db_host
# DB_PORT=5432
# DB_USER=db_user
# DB_PASSWORD=db_password

# --- 5. ESP32 & OTA Endpoint ---
OTA_ENDPOINT=/xiaozhi/ota
